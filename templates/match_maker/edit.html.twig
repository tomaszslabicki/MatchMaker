{% extends 'base.html.twig' %}

{% block title %}Edit MatchMaker{% endblock %}

{% block body %}
    <h1>Edit MatchMaker</h1>

    {{ include('match_maker/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('match_maker_index') }}">back to list</a>

    {{ include('match_maker/_delete_form.html.twig') }}
{% endblock %}
